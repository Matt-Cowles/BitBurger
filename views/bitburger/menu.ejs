<%- include('../partials/menuNav', { cart }) %> <% layout('layouts/boilerplate') %><%- include ('../partials/flash') %>

<link href="https://fonts.googleapis.com/css2?family=Knewave&family=Lemon&family=Vollkorn:wght@900&display=swap" rel="stylesheet" />
<link rel="stylesheet" href="/stylesheets/menu.css" />

<div class="menu-title card text-center m-auto mt-5 mb-3 fs-1 pt-5 pb-5">Our Menu</div>

<div class="container">
  <% let itemCounter = 1 %>

  <!-- Burger section of menu -->
  <div class="menu-subsection text-center align-center mb-5">Burgers!</div>
  <div class="food-container d-flex justify-content-start">
    <% for (let item of burgerList) { %>
    <!-- Use a partial to create the category section of menu -->
    <%- include('../partials/menu', { item, cart } )%> <% itemCounter++ %> <% } %>
  </div>

  <!-- Pizza section of menu -->
  <div class="menu-subsection text-center align-center mb-5">Pizza!</div>
  <div class="food-container d-flex justify-content-start">
    <% for (let item of pizzaList) { %>
    <!-- Use a partial to create the category section of menu -->
    <%- include('../partials/menu', { item, cart } )%> <% itemCounter++ %> <% } %>
  </div>

  <!-- Fries section of menu -->
  <div class="menu-subsection text-center align-center mb-5">Fries!</div>
  <div class="food-container d-flex justify-content-start">
    <% for (let item of friesList) { %>
    <!-- Use a partial to create the category section of menu -->
    <%- include('../partials/menu', { item, cart } )%> <% itemCounter++ %> <% } %>
  </div>

  <!-- Drinks section of menu -->
  <div class="menu-subsection text-center align-center mb-5">Drinks!</div>
  <div class="food-container d-flex justify-content-start">
    <% for (let item of drinksList) { %>
    <!-- Use a partial to create the category section of menu -->
    <%- include('../partials/menu', { item, cart } )%> <% itemCounter++ %> <% } %>
  </div>

  <!-- Dessert section of menu -->
  <div class="menu-subsection text-center align-center mb-5">Dessert!</div>
  <div class="food-container d-flex justify-content-start">
    <% for (let item of dessertList) { %>
    <!-- Use a partial to create the category section of menu -->
    <%- include('../partials/menu', { item, cart } )%> <% itemCounter++ %> <% } %>
  </div>
</div>

<!-- Reload to the same scroll position on the page when refreshed -->
<script>
  document.addEventListener("DOMContentLoaded", function (event) {
    const scrollpos = localStorage.getItem("scrollpos");
    if (scrollpos) window.scrollTo(0, scrollpos);
  });

  window.onbeforeunload = function (e) {
    localStorage.setItem("scrollpos", window.scrollY);
  };
</script>
